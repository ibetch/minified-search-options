import React from 'React';

const InputText = () => {
  return (
    <input type="search" id="site-search" name="q"
       aria-label="Search through site content" placeholder="read only" readOnly />
  );
}
export default InputText;